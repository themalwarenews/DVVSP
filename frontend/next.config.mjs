/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: false, // disables strict mode for demonstration
  // Intentionally no security headers or CSP
  // Add more insecure options as needed
};

export default nextConfig;
